[build]
buildCommand = "NODE_ENV=production pnpm install --no-frozen-lockfile && NODE_ENV=production pnpm --filter @workspace/doacao-solidaria run build && NODE_ENV=production pnpm --filter @workspace/api-server run build"

[deploy]
startCommand = "pnpm --filter @workspace/api-server run start"
